#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int lowerCase = 'a';
	int upperCase = 'A';
	int lowerCase = 'z';
	int upperCase = 'Z';

	while (lowerCase 'a'; 'z';)
	{
		putchar(lowerCase);
		lowerCase += 1;
	}
	while (upperCase = 'A'; 'Z';)
	{
	putchar(upperCase);
	upperCase += 1;
	}
	putchar('\n');
	return (0);
}
