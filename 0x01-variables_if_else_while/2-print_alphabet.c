#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for
	c = 'a';
	while c <= 'z';
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
