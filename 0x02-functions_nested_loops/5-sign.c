#include "main.h"

/**
 * sign - Print the sign of a number
 * @n: The number to be checked
 * Return: 1 (print positive) 0 (if otherwise) -1 (print negative)
 */

int print_sign(int n)

{
	if (n > 0)
	(
		_putchar(43);
		return (1);
	)
	else if
	(
		_putchar(45);
		return(-1);
	)
	else
	(
	 _putchar(48);
		return (0);
	)
}
