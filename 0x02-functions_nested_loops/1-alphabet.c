#include "main.h"

/**
 * main - entry point
 * print_alphabet:
 * Return: void
 */

void print_alphabet(void);
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(i);
	_putchar("\n");
	return (0);
}
